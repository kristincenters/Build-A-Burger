<li class="list-group-item">
    {{burger_name}}

    <button class="change-devoured"
        data-id="{{id}}"
        data-newdevoured="{{devoured}}">
        {{#if devoured}}Devour It!{{else}}Yum, Order Again!{{/if}}
    </button>

</li>