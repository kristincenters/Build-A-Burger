<li>
    {{burger_name}}

    <button class="change-devoured"
        data-id="{{id}}"
        data-newDevoured="{{DEVOURED}}">
        {{#if devoured}}DEVOURED{{else}}ORDER DONE{{/if}}
    </button>
</li>